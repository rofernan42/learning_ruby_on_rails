$("#books").html("<%= j render "books" %>");
<% # j = alias de escape_javascript ; escape_javascript est un helper qui gere les problematiques liees aux apostrophes, guillemets, retour a la ligne, etc. %>
